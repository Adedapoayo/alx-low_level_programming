#include "main.h"
/**
 * Return;: the line converted from the string
 */
int _atoi(char *s)
	int i, d, n, len, f, digit;

	i = 0;
	d = 0;
	n = 0;
	len = 0;
	f = 0;
	digit = 0;

	while (s[len] !- '\0')
	len++;
	while (i < len && f -- o)

	if (s[i] -- '.')
	++d;

	if 
